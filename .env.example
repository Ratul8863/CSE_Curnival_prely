# Server port. Falls back to 8000 when unset.
PORT=8000

# Optional Groq configuration. The API works without these.
# If GROQ_API_KEY is missing or invalid, the service falls back to rule-based output.
# GROQ_API_KEY=
# GROQ_MODEL=llama-3.1-8b-instant
